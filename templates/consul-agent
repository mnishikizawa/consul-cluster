CMD_OPTS="agent -datacenter={{ location }} -config-dir=/etc/consul -data-dir=/var/lib/consul -bind={{ ansible_all_ipv4_addresses[1] }} {% for i in groups['consul_servers'] %}-retry-join={{ hostvars[i].get('ansible_ssh_host') }} {% endfor %}"
